import {SHOP} from './collectionsItem'

const INITIAL={
  collections:SHOP
}
const collectionReducer=(state=INITIAL,action)=>{
  switch (action.type) {
    
    default:
     return state
  }
}

export default collectionReducer